<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Feature Bluetooth richieste -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- Permessi Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <!-- Permessi Internet per API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permessi Location (richiesti per Bluetooth su Android 12+) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_compass"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_menu_compass"
        android:supportsRtl="true"
        android:theme="@style/Theme.RFIDReader"
        android:usesCleartextTraffic="true"
        tools:targetApi="31"
        tools:replace="android:allowBackup,android:supportsRtl,android:theme,android:icon,android:label"
        tools:remove="android:appComponentFactory">

        <!-- LoginActivity è il nuovo LAUNCHER -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Inventory List -->
        <activity
            android:name=".InventoryListActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- New Inventory -->
        <activity
            android:name=".NewInventoryActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Nuovo Inventario"
            android:parentActivityName=".InventoryListActivity" />

        <!-- Inventory Scan (ex MainActivity) - OLD, da deprecare -->
        <activity
            android:name=".InventoryScanActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:keepScreenOn="true" />

        <!-- ✅ NUOVE ACTIVITY: Tre modalità separate -->
        <!-- Inventory Scan - NORMAL Mode -->
        <activity
            android:name=".InventoryScanNormalActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:keepScreenOn="true" />

        <!-- Inventory Scan - CHECKLIST Mode -->
        <activity
            android:name=".InventoryScanChecklistActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:keepScreenOn="true" />

        <!-- Inventory Scan - STOCK Mode (Last-Place) -->
        <activity
            android:name=".InventoryScanStockActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:keepScreenOn="true" />

        <!-- Settings -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Inventory Details -->
        <activity
            android:name=".InventoryDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Inventory Settings -->
        <activity
            android:name=".InventorySettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Inventory Settings"
            android:parentActivityName=".InventoryScanActivity" />

        <!-- ZXing barcode scanner forzato in portrait -->
        <activity
            android:name=".PortraitCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- Tag Info -->
        <activity
            android:name=".TagInfoActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Tag Info" />

        <activity
            android:name=".TagInfoDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Tag Details" />

        <activity
            android:name=".TagInfoOperationsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Operations" />

        <activity
            android:name=".TagDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Tag Details" />

        <!-- Locate Tag -->
        <activity
            android:name=".LocateActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Locate Tag"
            android:parentActivityName=".MainActivity" />

        <!-- RSSI Monitor -->
        <activity
            android:name=".RssiMonitorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="RSSI Monitor"
            android:parentActivityName=".LocateActivity" />

        <!-- Old MainActivity (kept for backward compatibility) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Letture -->
        <activity
            android:name=".LettureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Letture" />

        <!-- Letture Session (scan RFID) -->
        <activity
            android:name=".LettureSessionActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:keepScreenOn="true"
            android:label="Sessione Lettura" />

        <!-- Letture Details (items list with filters) -->
        <activity
            android:name=".LettureDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Dettagli Lettura" />
    </application>

</manifest>
